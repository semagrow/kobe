apiVersion: kobe.semagrow.org/v1alpha1
kind: FederatorTemplate
metadata:
  name: semagrowtemplate-geo-poly
spec:
  containers:
    - name: maincontainer 
      image: semagrow/semagrow:2.2.0
      ports:
      - containerPort: 8080
  port: 8080
  path: /SemaGrow/sparql
  confFromFileImage: alpine:3
  inputDumpDir: /semagrow/input
  outputDumpDir: /semagrow/output
  confImage: antru6/semagrow-init:gss-19-geo-poly
  inputDir: /input
  outputDir: /output
  fedConfDir: /etc/default/semagrow
